npm install --save-dev @types/node @types/express @types/jest @types/supertest @types/swagger-ui-express @types/ioredis @types/ioredis-mock

npm install swagger-ui-express openapi-types
npm install --save-dev @types/swagger-ui-express

npm install --save-dev supertest ioredis-mock @types/supertest @types/jest
npm install --save-dev @types/node @types/express @types/ioredis @types/express-rate-limit
npm test

npm i -D @types/node @types/express @types/ioredis @types/express-rate-limit @types/jest

npm install
npm run dev

need this example for linux terminsl
$body = '{ "phone": "+1234567890" }'
for ($i=0; $i -lt 7; $i++) { curl.exe -H "Content-Type: application/json" -X POST -d $body http://localhost:3000/otp/send; echo; }

npm i -D @types/node @types/express @types/express-rate-limit @types/pino @types/jest
npm test
npm run dev
# or build and run
npm run build; npm start
npm install

